cache:
  mount:
    - .git
    - /drone/docker

pipeline:
  build:
    image: ubuntu:14.04
    commands:
      - echo ${CI} 
  
  publish:
    localhost:5000/drone-ecr:
      registry: http://353893013802.dkr.ecr.us-east-1.amazonaws.com
      privileged: true
      repo: codebitsusers

  deploy:
    localhost:5000/drone-rancher:
      privileged: true
      service: wallet/wallet
      